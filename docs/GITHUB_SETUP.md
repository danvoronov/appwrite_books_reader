# 🚀 Настройка GitHub репозитория для автоматического развертывания

## 📋 Пошаговая инструкция

### 1. 🏗 Создайте GitHub репозиторий

```bash
# Если у вас еще нет репозитория
git init
git add .
git commit -m "Initial commit - REM by Gemini Appwrite Edition"
git branch -M main
git remote add origin https://github.com/YOUR-USERNAME/rem-by-gemini-appwrite.git
git push -u origin main
```

### 2. 🔑 Создайте API ключ в Appwrite

1. Откройте [Appwrite Console](https://cloud.appwrite.io/project-690f8b5b0012faa10454)
2. **Settings** → **API Keys** → **Create API Key**
3. **Name**: `GitHub Actions Deploy Key`
4. **Scopes** - выберите все необходимые права:
   ```
   ✅ functions.read
   ✅ functions.write
   ✅ files.read  
   ✅ files.write
   ✅ buckets.read
   ✅ buckets.write
   ✅ executions.read
   ✅ executions.write
   ```
5. **Скопируйте полученный ключ** (он понадобится на следующем шаге)

### 3. ⚙️ Настройте GitHub Secrets

1. Откройте ваш репозиторий на GitHub
2. **Settings** → **Secrets and variables** → **Actions**
3. **New repository secret**:
   - **Name**: `APPWRITE_API_KEY`
   - **Secret**: вставьте API ключ из предыдущего шага
4. **Add secret**

### 4. 🚀 Запустите развертывание

#### Автоматически (рекомендуется):
```bash
git add .
git commit -m "Add GitHub Actions deployment"
git push origin main
```

#### Или вручную:
1. Откройте репозиторий → **Actions**
2. **Deploy to Appwrite** → **Run workflow**
3. Нажмите **Run workflow**

## 📊 Мониторинг развертывания

### GitHub Actions
1. **Repository** → **Actions** tab
2. Наблюдайте за процессом развертывания в реальном времени
3. В случае ошибок - смотрите логи конкретного шага

### Appwrite Console
1. **Functions** → **rem-backend** → проверьте статус
2. **Storage** → **static-files** → убедитесь, что файлы загружены
3. **Storage** → **books** → проверьте ваши epub файлы

## ✅ После успешного развертывания

### 📱 Ваше приложение будет доступно по адресу:
```
https://fra.cloud.appwrite.io/v1/storage/buckets/static-files/files/index-html/view?project=690f8b5b0012faa10454
```

### 🧪 Тестирование:
1. Откройте основное приложение по ссылке выше
2. Откройте `TEST_STORAGE_INTEGRATION.html` для детального тестирования
3. Проверьте, что отображаются ваши реальные книги из bucket "books"

## 🔧 Обновления приложения

После любых изменений в коде:
```bash
git add .
git commit -m "Update: описание изменений"
git push origin main
```

GitHub Actions автоматически развернет обновления!

## ❌ Устранение проблем

### Проблема: "Session not found"
- **Решение**: API ключ неверный или не настроены правильные Scopes

### Проблема: "Function already exists" 
- **Решение**: Это нормально, GitHub Actions продолжит обновление

### Проблема: "Bucket already exists"
- **Решение**: Это нормально, GitHub Actions продолжит загрузку файлов

### Проблема: Приложение не открывается
1. Проверьте, что все файлы загружены в Storage
2. Убедитесь, что bucket "static-files" имеет права `read("any")`
3. Проверьте логи Function в Appwrite Console

## 🎯 Пример успешного развертывания

```
✅ Function created/updated: rem-backend
✅ Files uploaded to static-files bucket
✅ Environment variables configured
✅ App URL: https://fra.cloud.appwrite.io/v1/storage/buckets/static-files/files/index-html/view?project=690f8b5b0012faa10454
```

---

**🎉 После выполнения этих шагов ваше приложение будет автоматически развертываться при каждом push в репозиторий!**