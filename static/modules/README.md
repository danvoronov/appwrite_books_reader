# Модульная архитектура приложения

## Обзор

Приложение рефакторено по принципам **Чистого Кода** с разделением на логические модули. Каждый модуль отвечает за свою область ответственности.

## Структура модулей

### 📦 Utils.js
**Утилиты общего назначения**
- `formatSize()` - форматирование размеров файлов
- `formatNumber()` - форматирование чисел с разделителями
- `generateSessionId()` - генерация ID сессии
- `showError()` - отображение ошибок
- `escapeHtml()` / `escapeRegex()` - экранирование строк

### 🌐 UrlRouter.js
**Управление URL и маршрутизацией**
- Обновление URL при навигации
- Восстановление состояния из URL
- Обработка истории браузера (popstate)
- Парсинг параметров URL

### 📚 BookSelection.js
**Выбор и загрузка книг**
- Загрузка списка книг с сервера
- Рендеринг карточек книг
- Загрузка информации о книге
- Отображение статистики книги

### 📑 ChapterManager.js
**Управление главами**
- Рендеринг списка глав (с группировкой)
- Выбор/снятие выбора глав
- Фильтрация глав (все/обработанные/необработанные)
- Обновление кнопки обработки

### 📖 ChapterReader.js
**Читалка глав**
- Загрузка и отображение содержимого главы
- Парсинг и применение тегов/аннотаций
- Типографика (размер шрифта, межстрочный интервал)
- Правая панель с тегами и комментариями
- Обработка изображений из EPUB

**Ключевые функции:**
- `loadChapterContent()` - загрузка главы
- `fetchTags()` - получение тегов
- `annotateHtml()` - аннотирование текста
- `renderRightTags()` - рендер правой панели
- `applyReaderTypography()` - применение настроек шрифта

### ⚙️ ProcessingManager.js
**Управление обработкой глав**
- Запуск процесса обработки
- Остановка обработки
- Модальное окно процесса
- Скачивание саммари
- Интеграция с WebSocket и ProgressTracker

### 📊 ProgressTracker.js
**Отслеживание прогресса**
- Расчет общего прогресса
- Прогресс текущей главы
- Обновление прогресс-бара
- Логирование событий

### 🔌 WebSocketClient.js
**WebSocket клиент**
- Подключение к серверу
- Получение обновлений прогресса в реальном времени
- Обработка сообщений от сервера

### 🎯 BookProcessor.js
**Главный координатор (фасад)**
- Инициализация всех модулей
- Координация взаимодействия между модулями
- Управление состоянием приложения
- Обработка событий UI

## Диаграмма зависимостей

```
app_new.js (точка входа)
    └── BookProcessor.js (координатор)
        ├── Utils.js
        ├── UrlRouter.js
        ├── BookSelection.js
        │   └── Utils.js
        ├── ChapterManager.js
        │   └── Utils.js
        ├── ChapterReader.js
        │   └── Utils.js
        └── ProcessingManager.js
            ├── WebSocketClient.js
            └── ProgressTracker.js
```

## Принципы разработки

### ✅ Применённые принципы Чистого Кода:

1. **Single Responsibility Principle (SRP)** - каждый модуль отвечает за одну область
2. **DRY (Don't Repeat Yourself)** - общие утилиты вынесены в Utils.js
3. **Separation of Concerns** - логика разделена по функциональным областям
4. **Модульность** - ES6 модули с явным импортом/экспортом
5. **Читаемость** - понятные имена функций и переменных
6. **Малые функции** - каждая функция делает одно дело

### 🔄 До рефакторинга:
- **1 файл** - 1434 строки
- Один огромный класс со всей логикой
- Сложно поддерживать и тестировать

### ✨ После рефакторинга:
- **9 модулей** - средний размер ~250 строк
- Чёткое разделение ответственности
- Легко тестировать и расширять
- Повторное использование кода

## Как использовать

### Подключение в HTML:
```html
<script type="module" src="app_new.js"></script>
```

### Создание нового модуля:
```javascript
import { Utils } from './Utils.js';

export class MyModule {
    constructor(bookProcessor) {
        this.bp = bookProcessor;
    }
    
    myMethod() {
        // ваш код
    }
}
```

### Интеграция в BookProcessor:
```javascript
import { MyModule } from './MyModule.js';

constructor() {
    // ...
    this.myModule = new MyModule(this);
}
```

## Тестирование

Модульная структура позволяет легко тестировать каждый компонент отдельно:

```javascript
// Пример unit-теста
import { Utils } from './Utils.js';

test('formatSize форматирует большие числа', () => {
    expect(Utils.formatSize(5000)).toBe('5.0k');
});
```

## Расширение функциональности

Для добавления новой функции:

1. Определите область ответственности (UI, обработка данных, API и т.д.)
2. Найдите соответствующий модуль или создайте новый
3. Добавьте метод в модуль
4. Подключите через BookProcessor если нужен доступ из других модулей

## Производительность

- Модули загружаются асинхронно (ES6 modules)
- Код разделён - браузер может кэшировать модули отдельно
- Параллельная загрузка данных (Promise.all)
- Ленивая загрузка контента

## Совместимость

Требуется поддержка ES6 modules:
- Chrome 61+
- Firefox 60+
- Safari 11+
- Edge 16+

Для старых браузеров можно использовать транспиляцию (Babel + Webpack).
