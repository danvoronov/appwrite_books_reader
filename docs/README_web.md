# REM by Gemini - Веб Версия

## 🚀 Запуск веб-сервера

### Установка зависимостей
```bash
npm install
```

### Запуск веб-интерфейса
```bash
npm run web
```

После запуска откройте браузер и перейдите по адресу: **http://localhost:3456**

## 📋 Функциональность

### ✨ Возможности веб-версии:
- 📚 **Интерактивный выбор книги** из списка доступных epub файлов
- 📖 **Просмотр информации о главах** с размером контента и статусом обработки
- ✅ **Выбор глав для обработки** (одну или несколько)
- ⚡ **Real-time прогресс** обработки через WebSocket
- 🔄 **Автоматическое определение** уже обработанных глав
- 💾 **Принудительное чтение** из epub файлов (опция)

### 🎯 Преимущества веб-версии:
- **Удобный интерфейс** - не нужно работать с командной строкой
- **Визуальный выбор** - наглядный список книг и глав
- **Прогресс в реальном времени** - видите процесс обработки
- **Мультиплатформенность** - работает в любом браузере
- **Параллельная работа** - можно открыть несколько вкладок

## 🔧 Техническая информация

### Стек технологий:
- **Backend**: Fastify.js (Node.js)
- **Frontend**: Vanilla JavaScript + WebSocket
- **Порт**: 3456
- **Real-time**: WebSocket соединение

### API Endpoints:
- `GET /api/books` - получить список доступных книг
- `POST /api/book/info` - получить информацию о книге и главах
- `POST /api/process` - запустить обработку выбранных глав
- `GET /ws` - WebSocket для real-time обновлений

## 📁 Структура файлов

```
public/
├── index.html      # Основной HTML интерфейс
└── app.js         # JavaScript логика frontend

server.js          # Fastify сервер
package.json       # Добавлены веб-зависимости
```

## 🎨 Интерфейс

### Шаг 1: Выбор книги
- Отображение всех доступных книг из папки `../epub/`
- Выделение последней выбранной книги ⭐
- Опция принудительного чтения из epub

### Шаг 2: Выбор глав
- Список всех глав с информацией о размере
- Индикация уже обработанных глав ✅
- Кнопки "Выбрать все" / "Снять выделение"
- Предупреждение для больших глав ⚠️

### Шаг 3: Обработка
- Real-time лог процесса обработки
- Прогресс по каждой главе
- Результаты с индикацией успеха/ошибок

## 🔄 Сравнение с CLI версией

| Функция | CLI | Web |
|---------|-----|-----|
| Выбор книги | Текстовое меню | Визуальные карточки |
| Выбор глав | Номера глав | Интерактивные карточки |
| Прогресс | Консольный вывод | Real-time веб-лог |
| Удобство | Для разработчиков | Для всех пользователей |
| Автоматизация | Параметры командной строки | Веб-интерфейс |

## 🛠 Запуск обеих версий

```bash
# CLI версия (как было)
npm start

# Веб версия (новая)
npm run web
```

## ⚡ Быстрый старт

1. Убедитесь, что папка `../epub/` содержит .epub файлы
2. Установите зависимости: `npm install`
3. Запустите веб-сервер: `npm run web`
4. Откройте http://localhost:3456 в браузере
5. Выберите книгу и главы для обработки
6. Наблюдайте за процессом в реальном времени!